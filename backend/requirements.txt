fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
sqlalchemy==2.0.23
alembic==1.12.1
aiosqlite==0.19.0
greenlet==3.0.3
python-multipart==0.0.6
# python-magic removed - using extension-based fallback
pypdf==6.6.0
pdfminer.six==20221105
PyMuPDF==1.23.6
pytesseract==0.3.10
Pillow>=10.0.0  # Image processing for OCR and fraud detection
python-docx==1.1.0
openpyxl==3.1.2
httpx==0.25.2
urllib3<2.0  # Pin to v1.x for LibreSSL compatibility
pydantic-settings==2.1.0
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-mock==3.12.0
# BERT embeddings for semantic document classification
# NOTE: sentence-transformers will install PyTorch. By default it installs GPU version (2GB+ CUDA packages).
# To install CPU-only version (much smaller, no NVIDIA dependencies), run BEFORE pip install:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# Then run: pip install -r requirements.txt
# Or if you have GPU and want GPU acceleration, just run: pip install -r requirements.txt
huggingface-hub>=0.34.0,<1.0
sentence-transformers>=2.2.0
scikit-learn>=1.3.0
numpy>=1.24.0